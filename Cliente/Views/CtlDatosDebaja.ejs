<!-- CtlDatosDebaja.ejs -->
<link rel="stylesheet" type="text/css" href="/css/DatosDebaja.css">
<style>
  .vista {
    display: none;
  }
</style>
<section class="content">
 
    <div class="row center  mb-5" style="box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1); border-radius: 8px; width: auto; margin-top: 10px;">
      <div class="grid-actions">
        <button id="btnMostrarCategoria" class="button" style="--clr: #00ad54;" onclick="mostrarVista('vista1')">
            <span class="button-decor"></span>
            <div class="button-content">
                <div class="button__icon">
                    <i class="fas fa-tag"></i>
                </div>
                <span class="button__text">Mostrar Categoria</span>
            </div>
        </button>
    
        <button id="btnMostrarBodega" class="button" style="--clr: #00ad54;" onclick="mostrarVista('vista2')">
            <span class="button-decor"></span>
            <div class="button-content">
                <div class="button__icon">
                    <i class="fas fa-warehouse"></i>
                </div>
                <span class="button__text">Mostrar Bodega</span>
            </div>
        </button>
    
        <button id="btnMostrarProducto" class="button " style="--clr: #00ad54;" onclick="mostrarVista('vista3')">
            <span class="button-decor"></span>
            <div class="button-content">
                <div class="button__icon">
                    <i class="fas fa-shopping-cart"></i>
                </div>
                <span class="button__text">Seleccionar Producto</span>
            </div>
        </button>
    
        <button id="btnMostrarProveedor" class="button" style="--clr: #00ad54;" onclick="mostrarVista('vista4')">
            <span class="button-decor"></span>
            <div class="button-content">
                <div class="button__icon">
                    <i class="fas fa-truck"></i>
                </div>
                <span class="button__text">Mostrar Proveedor</span>
            </div>
        </button>
    
        <button id="btnMostrarColores" class="button" style="--clr: #00ad54;" onclick="mostrarVista('vista5')">
            <span class="button-decor"></span>
            <div class="button-content">
                <div class="button__icon">
                    <i class="fas fa-palette"></i>
                </div>
                <span class="button__text">Mostrar Colores</span>
            </div>
        </button>
    
        <button id="btnMostrarPromociones" class="button" style="--clr: #00ad54;" onclick="mostrarVista('vista6')">
            <span class="button-decor"></span>
            <div class="button-content">
                <div class="button__icon">
                    <i class="fas fa-tags"></i>
                </div>
                <span class="button__text">Mostrar Promociones</span>
            </div>
        </button>
    
        <button id="btnMostrarTallas" class="button" style="--clr: #00ad54;" onclick="mostrarVista('vista7')">
            <span class="button-decor"></span>
            <div class="button-content">
                <div class="button__icon">
                    <i class="fas fa-ruler-combined"></i>
                </div>
                <span class="button__text">Mostrar Tallas</span>
            </div>
        </button>
    </div>
    
    
    </div>

    <div class="row center" style="margin-left: 300px;">
      <div id="vista1" class="vista">
        <div class="card">
          <div class="header">
            <h2 class="title">Categorías Dadas de Baja</h2>
          </div>

          <!-- Agregar un campo de búsqueda -->
          <div class="search-container">
            <input type="text" id="searchInput" placeholder="Buscar">
          </div>

          <div class="user__container">
            <% categoriasInactivas.forEach(categoria=> { %>
              <div class="user">
                <!-- Aquí puedes personalizar según tus datos de la categoría -->
                <div class="icon"></div> <!-- Reemplazar la imagen con un icono -->
                <div class="user__content">
                  <div class="text">
                    <span class="name">
                      <%= categoria.Nombre %>
                    </span>
                  </div>
                  <form action="/api/categorias/activar" method="POST">
                    <input type="hidden" name="codigo" value="<%= categoria.Codigo %>">
                    <button class="follow" type="submit">Activar</button>
                  </form>
                </div>
              </div>
              <% }); %>
          </div>
        </div>
      </div>

      <div id="vista2" class="vista">
        <div class="card">
          <div class="header">
            <h2 class="title">Bodegas Dadas de Baja</h2>
          </div>

          <!-- Agregar un campo de búsqueda -->
          <div class="search-container">
            <input type="text" id="searchInput" placeholder="Buscar">
          </div>

          <div class="user__container">
            <% BodegaInactivas.forEach(Bodega=> { %>
              <div class="user">
                <!-- Aquí puedes personalizar según tus datos de la categoría -->
                <div class="icon"></div> <!-- Reemplazar la imagen con un icono -->
                <div class="user__content">
                  <div class="text">
                    <span class="name">
                      <%= Bodega.Nombre %>
                    </span>
                  </div>
                  <form action="/api/bodega/activar" method="POST">
                    <input type="hidden" name="codigo" value="<%= Bodega.Codigo %>">
                    <button class="follow" type="submit">Activar</button>
                  </form>
                </div>
              </div>
              <% }); %>
          </div>
        </div>
      </div>

      <div id="vista3" class="vista">
        <div class="card">
          <div class="header">
            <h2 class="title">Productos Dados de Baja</h2>
          </div>

          <!-- Agregar un campo de búsqueda -->
          <div class="search-container">
            <input type="text" id="searchInput" placeholder="Buscar">
          </div>

          <div class="user__container">
            <% ProductosInactivos.forEach(Producto=> { %>
              <div class="user">
                <!-- Aquí puedes personalizar según tus datos del producto -->
                <div class="icon"></div> <!-- Reemplazar la imagen con un icono -->
                <div class="user__content">
                  <div class="text">
                    <span class="name">
                      <%= Producto.Nombre %>
                    </span>
                  </div>
                  <form action='/api/producto/activar' method="POST">
                    <input type="hidden" name="codigo" value="<%= Producto.Codigo %>">
                    <button class="follow" type="submit">Activar</button>
                  </form>
                </div>
              </div>
              <% }); %>
          </div>
        </div>
      </div>

      <div id="vista4" class="vista">
        <div class="card">
          <div class="header">
            <h2 class="title">Proveedores Dados de Baja</h2>
          </div>

          <!-- Agregar un campo de búsqueda -->
          <div class="search-container">
            <input type="text" id="searchInput" placeholder="Buscar">
          </div>

          <div class="user__container">
            <% ProveedoresInactivos.forEach(Proveedor=> { %>
              <div class="user">
                <!-- Personalización según los datos del proveedor -->
                <div class="icon"></div> <!-- Reemplazar la imagen con un icono -->
                <div class="user__content">
                  <div class="text">
                    <span class="name">
                      <%= Proveedor.Nombre %>
                    </span>
                  </div>
                  <form action="/api/proveedor/activar" method="POST">
                    <input type="hidden" name="codigo" value="<%= Proveedor.Codigo %>">
                    <button class="follow" type="submit">Activar</button>
                  </form>
                </div>
              </div>
              <% }); %>
          </div>
        </div>
      </div>

      <div id="vista5" class="vista">
        <div class="card">
          <div class="header">
            <h2 class="title">Colores Dados de Baja</h2>
          </div>

          <!-- Agregar un campo de búsqueda -->
          <div class="search-container">
            <input type="text" id="searchInput" placeholder="Buscar">
          </div>

          <div class="user__container">
            <% ColoresInactivos.forEach(Colores=> { %>
              <div class="user">
                <!-- Personalización según los datos del proveedor -->
                <div class="icon"></div> <!-- Reemplazar la imagen con un icono -->
                <div class="user__content">
                  <div class="text">
                    <span class="name">
                      <%= Colores.Color %>
                    </span>
                  </div>
                  <form action="/api/colores/activar" method="POST">
                    <input type="hidden" name="codigo" value="<%= Colores.Codigo %>">
                    <button class="follow" type="submit">Activar</button>
                  </form>
                </div>
              </div>
              <% }); %>
          </div>
        </div>
      </div>

      <div id="vista6" class="vista">
        <div class="card">
          <div class="header">
            <h2 class="title">Promociones Dadas de Baja</h2>
          </div>

          <!-- Agregar un campo de búsqueda -->
          <div class="search-container">
            <input type="text" id="searchInput" placeholder="Buscar">
          </div>

          <div class="user__container">
            <% PromocionesInactivas.forEach(Promociones=> { %>
              <div class="user">
                <!-- Personalización según los datos del proveedor -->
                <div class="icon"></div> <!-- Reemplazar la imagen con un icono -->
                <div class="user__content">
                  <div class="text">
                    <span class="name">
                      <%= Promociones.Nombre %>
                    </span>
                  </div>
                  <form action="/api/promociones/activar" method="POST">
                    <input type="hidden" name="codigo" value="<%= Promociones.Codigo %>">
                    <button class="follow" type="submit">Activar</button>
                  </form>
                </div>
              </div>
              <% }); %>
          </div>
        </div>
      </div>

      <div id="vista7" class="vista">
        <div class="card">
          <div class="header">
            <h2 class="title">Tallas Inactivas</h2>
          </div>
      
          <!-- Agregar un campo de búsqueda -->
          <div class="search-container">
            <input type="text" id="searchInput" placeholder="Buscar">
          </div>
      
          <div class="user__container">
            <% TallasInactivas.forEach(Talla => { %>
              <div class="user">
                <!-- Personalización según los datos de la talla -->
                <div class="icon"></div> <!-- Reemplazar la imagen con un icono -->
                <div class="user__content">
                  <div class="text">
                    <span class="name">
                      <%= Talla.Numero %>
                    </span>
                  </div>
                  <form action="/api/tallas/activar" method="POST">
                    <input type="hidden" name="codigo" value="<%= Talla.Codigo %>">
                    <button class="follow" type="submit">Activar</button>
                  </form>
                </div>
              </div>
            <% }); %>
          </div>
        </div>
      </div>
      


    </div>
</section>

<script>
  document.getElementById('searchInput').addEventListener('input', function () {
    var searchTerm = this.value.toLowerCase();
    var tableRows = document.querySelectorAll('.user__container .user');

    tableRows.forEach(function (row) {
      var rowData = row.textContent.toLowerCase();
      if (rowData.includes(searchTerm)) {
        row.style.display = 'flex'; // Mostrar fila
      } else {
        row.style.display = 'none'; // Ocultar fila
      }
    });
  });

  function mostrarVista(idVista) {
    // Oculta todas las vistas
    var vistas = document.getElementsByClassName('vista');
    for (var i = 0; i < vistas.length; i++) {
      vistas[i].style.display = 'none';
    }

    // Carga la vista si aún no se ha cargado
    var vistaSeleccionada = document.getElementById(idVista);
    if (vistaSeleccionada && vistaSeleccionada.innerHTML.trim() === "") {
      // Puedes cargar contenido dinámicamente aquí, por ejemplo, con AJAX
      cargarContenido(idVista);
    }

    // Muestra la vista deseada
    if (vistaSeleccionada) {
      vistaSeleccionada.style.display = 'block';
    }
  }


</script>